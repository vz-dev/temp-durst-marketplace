Sehr geehrte/r Händler/in,

leider ist der Import der Zeitfenster für die Filiale "{{ mail.branch.name }}" fehlgeschlagen.

Fehlerhafte Zeile:

{{ mail.exception.failedItem|join(constant('Pyz\\Zed\\DeliveryArea\\Business\\Import\\TimeSlotCsvImporter::IMPORT_CSV_DELIMITER')) }} ({{ mail.exception.failedRow }})

Bitte prüfen Sie die verwendete Datei, die Sie im Anhang finden, und korrigieren Sie diesen und eventuelle weitere Fehler. Sollten Sie den Fehler nicht beheben können und Unterstützung benötigen, wenden Sie sich bitte an den Händler-Support.

Freundliche Grüße
Ihr Durst-Team
